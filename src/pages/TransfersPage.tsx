import { PageSection } from './PageSection';

export function TransfersPage() {
  return (
    <PageSection heading="Custody movements">
      <p>Declare item movement between workshop, transit, and field custody.</p>
      <p className="mt-2">Placeholder: transfer feed with declaration state and timestamps.</p>
    </PageSection>
  );
}
